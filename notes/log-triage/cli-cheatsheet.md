# 🧪 Log Triage — CLI Cheat Sheet

## 🧠 Ментальная модель
SOURCE → FILTER → EXTRACT → COUNT → SORT → LIMIT  

---
## 📦 Базовые команды

**`cat access.log`**  
→ выводит файл целиком  
→ источник данных для пайпов

**`less access.log`**  
→ постраничный просмотр  
→ ручной анализ больших логов

**`grep "Failed password" auth.log`**  
→ фильтрация строк по шаблону  
→ выделение нужных событий

**`grep -E "sshd|sudo" auth.log`**  
→ фильтрация по нескольким шаблонам (regex)  
→ группировка типов событий

**`cut -d' ' -f1 access.log`**  
→ извлекает 1-е поле по пробелу  
→ получение IP-адресов

**`cut -d'"' -f2 access.log`**  
→ извлекает поле между кавычками  
→ получение HTTP-метода и пути

**`sort`**  
→ сортировка строк  
→ обязательна перед `uniq`

**`sort -nr`**  
→ числовая сортировка по убыванию  
→ вывод TOP значений

**`uniq`**  
→ удаляет дубликаты соседних строк  
→ применяется после `sort`

**`uniq -c`**  
→ считает количество повторений  
→ подсчёт IP / путей / пользователей

**`wc -l`**  
→ считает строки  
→ оценка объёма событий

**`head -10`**  
→ первые строки  
→ ограничение вывода

**`tail -20`**  
→ последние строки  
→ просмотр свежих событий

**`|` (pipe)**  
→ передаёт вывод одной команды на вход другой  
→ основа всего анализа

---

## 🔥 Практические one-liners (nginx)

**TOP IP-адреса**
```bash
cut -d' ' -f1 access.log | sort | uniq -c | sort -nr | head
```
→ поиск ботов и сканеров

**TOP URL-пути (recon)**
```bash
cut -d'"' -f2 access.log | cut -d' ' -f2 | sort | uniq -c | sort -nr
```
→ выявление `/admin`, `/.env`, `/login`

**HTTP-методы**
```bash
cut -d'"' -f2 access.log | cut -d' ' -f1 | sort | uniq -c | sort -nr
```
→ человек vs автоматизация

**User-Agent**
```bash
cut -d'"' -f6 access.log | sort | uniq -c | sort -nr
```
→ определение инструментов (curl, sqlmap)

**Действия одного IP**
```bash
grep "^127.0.0.1 " access.log
```
→ реконструкция поведения атакующего

---

